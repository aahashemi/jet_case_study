dbt_jet_case_study:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: 'database'
      schema: 'main'
      schemas_and_paths:
        main: '../data/xkcd.db'
      schema_directory: 'file_path'
      